{
  "name": "vertical-farming",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "live-server --open",
    "vercel-build": "git lfs install && git lfs pull && vite build"

  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.11"
  },
  "dependencies": {
    "postprocessing": "^6.37.1",
    "three": "^0.157.0"
  }
}
